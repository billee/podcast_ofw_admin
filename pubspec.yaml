name: podcasts_ofw_admin
description: Admin panel for OFW Podcasts

publish_to: 'none'

version: 1.0.0+0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^2.24.1
  firebase_auth: ^4.17.1
  cloud_firestore: ^4.15.1
  provider: ^6.1.1
  fluttertoast: ^8.2.4
  supabase: ^1.10.4  # Add this for Supabase



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true